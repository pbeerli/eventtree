dnl Process this file with autoconf to produce a configure script.
dnl Created by Anjuta - will be overwritten
dnl If you don't want it to overwrite it,
dnl 	Please disable it in the Anjuta project configuration

AC_INIT(configure.in)
AM_INIT_AUTOMAKE("EventTree", "0.99")
AM_CONFIG_HEADER(config.h)
AM_MAINTAINER_MODE

AC_PROG_CC
AC_PROG_CPP
AC_PROG_CXX
AC_PROG_RANLIB

AM_OPTIONS_WXCONFIG
AM_PATH_WXCONFIG(2.6.0, wxWin=1, wxWin=0, core base xml svg)
     if test "$wxWin" != 1; then
	AC_MSG_ERROR([
	   wxWidgets must be installed on your system
	   but wx-config script couldn't be found.
	
	   Please check that wx-config is in path, the directory
	   where wxWidgets libraries are installed (returned by
	   'wx-config - -libs' command) is in LD_LIBRARY_PATH or
	   equivalent variable and wxWidgets version is 2.6.0 or above.
	])
     else
	# do not update CPPFLAGS nor CXXFLAGS otherwise we'll get the
	# same settings repetead three times (since Makefile.in merges these
	# two variables with $WX_CPPFLAGS automatically)
	#CPPFLAGS="$CPPFLAGS"
	CXXFLAGS="$CXXFLAGS"
	CFLAGS="$CFLAGS $WX_CFLAGS_ONLY"
	LDFLAGS="$LDFLAGS $WX_LIBS"
     fi


dnl Set PACKAGE_LOCALE_DIR in config.h.
if test "x${prefix}" = "xNONE"; then
  AC_DEFINE_UNQUOTED(PACKAGE_LOCALE_DIR, "${ac_default_prefix}/${DATADIRNAME}/locale", [Package local directory])
else
  AC_DEFINE_UNQUOTED(PACKAGE_LOCALE_DIR, "${prefix}/${DATADIRNAME}/locale", [Package local directory])
fi

dnl Set PACKAGE SOURCE DIR in config.h.
packagesrcdir=`cd $srcdir && pwd`

dnl Set PACKAGE PREFIX
if test "x${prefix}" = "xNONE"; then
  packageprefix=${ac_default_prefix}
else
  packageprefix=${prefix}
fi

dnl Set PACKAGE DATA & DOC DIR
packagedatadir=share
packagedocdir=doc/${PACKAGE}

dnl Set PACKAGE DIRS in config.h.
packagepixmapsdir=${packagedatadir}/pixmaps
packagehelpdir=${packagedatadir}/help
packagemenudir=${packagedatadir}

dnl Subst PACKAGE_DATA_DIR.
NO_PREFIX_PACKAGE_DATA_DIR="${packagedatadir}"
AC_SUBST(NO_PREFIX_PACKAGE_DATA_DIR)
PACKAGE_DATA_DIR="${packageprefix}/${packagedatadir}"
AC_SUBST(PACKAGE_DATA_DIR)

dnl Subst PACKAGE_DOC_DIR.
NO_PREFIX_PACKAGE_DOC_DIR="${packagedocdir}"
AC_SUBST(NO_PREFIX_PACKAGE_DOC_DIR)
PACKAGE_DOC_DIR="${packageprefix}/${packagedocdir}"
AC_SUBST(PACKAGE_DOC_DIR)

dnl Subst PACKAGE_PIXMAPS_DIR.
NO_PREFIX_PACKAGE_PIXMAPS_DIR="${packagepixmapsdir}"
AC_SUBST(NO_PREFIX_PACKAGE_PIXMAPS_DIR)
PACKAGE_PIXMAPS_DIR="${packageprefix}/${packagepixmapsdir}"
AC_SUBST(PACKAGE_PIXMAPS_DIR)

dnl Subst PACKAGE_HELP_DIR.
NO_PREFIX_PACKAGE_HELP_DIR="${packagehelpdir}"
AC_SUBST(NO_PREFIX_PACKAGE_HELP_DIR)
PACKAGE_HELP_DIR="${packageprefix}/${packagehelpdir}"
AC_SUBST(PACKAGE_HELP_DIR)

dnl Subst PACKAGE_MENU_DIR.
NO_PREFIX_PACKAGE_MENU_DIR="${packagemenudir}"
AC_SUBST(NO_PREFIX_PACKAGE_MENU_DIR)
PACKAGE_MENU_DIR="${packageprefix}/${packagemenudir}"
AC_SUBST(PACKAGE_MENU_DIR)

AC_DEFINE([PACKAGE_DATA_DIR],[], "${packageprefix}/${packagedatadir}")
AC_DEFINE([PACKAGE_DOC_DIR],[], "${packageprefix}/${packagedocdir}")
AC_DEFINE([PACKAGE_PIXMAPS_DIR],[], "${packageprefix}/${packagepixmapsdir}")
AC_DEFINE([PACKAGE_HELP_DIR], [],"${packageprefix}/${packagehelpdir}")
AC_DEFINE([PACKAGE_MENU_DIR], [],"${packageprefix}/${packagemenudir}")
AC_DEFINE([PACKAGE_SOURCE_DIR],[], "${packagesrcdir}")


AC_OUTPUT([
Makefile
src/Makefile
src/haru/Makefile
src/ncl/Makefile
])

